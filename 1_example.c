#include <stdio.h>
/*Removed unnecessary Includes*/

int main(void)
{
    printf("Hello world\n");
    // return 1; the return value of 1 means that an error occured, 0 means that the program ran without issues
    return 0;
}